<?php

// We check if the globals aren't empty
if (!empty($_POST['clipFile']) && !empty($_POST['clipThumbnail']) && !empty($_POST['clipName']) && !empty($_POST['clipDescription'])) {
    // Traitement
}
